includes:
  - ./defaults.yaml
  
model_config:
  unimodal_efficientnet:

    image_encoder:
      name: efficientnet-b7

    classifier:
      params:
        in_dim: 2560
        out_dim: 2
        hidden_dim: 768
        num_layers: 1

training:
  batch_size: 16
