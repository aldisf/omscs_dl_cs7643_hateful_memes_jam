includes:
  - ./defaults.yaml
  
model_config:
  late_fusion_efficientnet:

    image_encoder:
      name: efficientnet-b4

    image_classifier:
      params:
        in_dim: 1792

training:
  batch_size: 32